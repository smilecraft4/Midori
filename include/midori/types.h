#ifndef MIDORI_TYPES_H
#define MIDORI_TYPES_H

#include <cstdint>

using Tile = std::uint32_t;
static constexpr auto MAX_TILE = UINT32_MAX;

using Layer = std::uint32_t;
static constexpr auto MAX_LAYER = UINT32_MAX;

#endif